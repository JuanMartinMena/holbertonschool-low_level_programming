#include "main.h"
/**
 * print_last_digit - print
 * @n: numero random
 *
 * Return: Last number
 */
int print_last_digit(int n)
{
	int last = n % 10;

	while (n < 0)
{
	_putchar(-last);
}
{
	while (n >= 0)
{
	_putchar(last);
}
	return (0);
}
}
