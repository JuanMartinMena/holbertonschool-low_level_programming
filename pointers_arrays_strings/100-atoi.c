#include "main.h"
/**
 * _atoi - print
 *
 * @s: variable
 *
 *
 * Return: void
 */
int _atoi(char *s)
{
	int largo = 0;
	char menos = '-';
	char mas = '+';
	int numero = 
	while (s[largo] != '\0') //esto es para ver el largo del string
	{
		largo++;
	}
	if (largo == 65 - 90 || largo == menos, mas) //si los valores del string son numeros/+/- que haga lo de adentro 
	{
		if () //si uno es mayor al otro que me lo cambie
		{
		//printear signo de menos
		}
		else
		{
		//printear printear singo de mas
		}
		//printear numero
	}
	else //si no es numero/+/- retorne 0
	{
	return (0);
	}
}
