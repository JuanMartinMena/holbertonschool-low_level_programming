#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char inicio = 'a';

	while (inicio < 'z')
		if (inicio != 'e' && inicio != 'q')
{
		putchar(inicio);
}
{
		inicio++;
}
	return (0);
}
