#include <stdio.h>
/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int Base10 = '1';

	for (Base10 = 1; Base10 <= 9; Base10++)
{
	putchar(Base10);
}
	putchar('\n');

	return (0);
}
