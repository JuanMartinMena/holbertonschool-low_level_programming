#include <stdio.h>
/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int Base10;

	for (Base10 = 0; Base10 <= 9; Base10++)
{
	printf("%d", Base10);
}
	printf("\n");

	return (0);
}
