#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int letra

	for (letra = 'z'; letra <= 'a'; letra++)
{
	putchar(letra + '0');
}
	putchar("\n");

	return (0)
}
